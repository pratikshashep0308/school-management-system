# controllers / ledger

Scaffolded at P1.1. Not implemented.

All models here MUST set `{ collection: 'fms_...' }` explicitly.
All money fields are INTEGER PAISE.
No hard deletes - soft-cancel via `status`.
